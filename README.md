# Dittert conjecture: n=7 extension note

Materials for the n=7 residual-core / p=1 route (extension note + certificate script).

## Files

| File | Description |
|------|-------------|
| `dittert_n7_extension_note.pdf` | Extension note (PDF) |
| `dittert_n7_extension_note.tex` | Source TeX |
| `dittert_n7_certificates.py` | Certificate / verification script |

## Status

This package records a reduced route for n=7 (two-marginal / p=1 structure). It is **not** claimed as a fully completed independent theorem until residual certificates are fully written and checked. See the note for details.

## Run certificates

```bash
python3 dittert_n7_certificates.py
```
